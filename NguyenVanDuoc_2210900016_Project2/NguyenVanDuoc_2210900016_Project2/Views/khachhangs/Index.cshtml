@model IEnumerable<NguyenVanDuoc_2210900016_Project2.Models.khachhang>

@{
    ViewBag.Title = "khachhang";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Danh sách khách hàng</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaKhach)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoTen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SDT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiaChi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaKhach)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoTen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SDT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DiaChi)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id = item.MaKhach }, new { @class = "btn btn-success" }) |
            @Html.ActionLink("Cập nhật", "Details", new { id = item.MaKhach }, new { @class = "btn btn-info" }) |
            @Html.ActionLink("Xóa", "Delete", new { id = item.MaKhach }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
